# CLAUDE.md — 프로젝트 컨텍스트

이 파일은 Claude Code가 작업 시작 시 자동으로 읽는 프로젝트 안내서다.
새 세션에서 매번 설명하지 않아도 되도록 핵심 맥락을 여기 적어둔다.

---

## 프로젝트 한 줄 정의

서울시 빅데이터 캠퍼스 공모전 출품작.
**전세거래 취약도 기반 전세사기 선행위험 조기경보 모델**.
구 단위로 검증된 위험 특징을 동·격자 단위로 내려서, 아직 피해 통계엔
안 잡혔지만 위험 신호가 쌓이는 "다음 위험지역(예방자원 우선지역)"을 찾는다.

작년 수상작 GeoPath의 공간분석 골격(격자DB→정규화→KDE→K-Means→중첩)을 차용한다.

## 절대 원칙 (작업 시 항상 지킬 것)

1. **연도 통일**: 입력 변수(전세가율·신축빌라·공시지가·인구)는 가능한 한
   최신(2025~2026)으로 맞춘다. 건축물대장은 반드시 최신 개방대장을 쓴다
   (2016년본 금지 — 신축 변수가 망가짐).
2. **피해 데이터의 역할**: `서울특별시_자치구별_전세사기_발생건수.csv`(2023~25)는
   정답이 아니라 **검증용 닻**이다. 직접 예측 타깃으로 쓰지 말 것.
3. **분석 단위**: 메인은 250m 격자. 학습은 행정동(424개). 검증은 자치구(25개).
   자치구 25개로는 머신러닝 회귀 금지(표본 부족) → 상관·순위 검증만.
4. **낙인 방지**: 변수명·결과물에 "사기 많은 동네" 같은 표현 금지.
   "취약지역 / 예방자원 우선지역 / 선행위험"으로 표기.
5. **인코딩**: 한국 공공데이터 CSV는 대부분 CP949(EUC-KR). 읽을 때 항상
   `encoding='cp949'` 먼저 시도하고 실패 시 utf-8.
6. **좌표계**: 거리·면적 계산은 EPSG:5179 또는 5186(미터 단위)로 변환 후 수행.
   시각화는 EPSG:4326(위경도)로 변환.

## 폴더 구조

```
data/raw/        캠퍼스 반출 원본 (전세/공시지가/인구/이동/대장) — 폐쇄망에서만
data/external/   외부 다운로드 (국토부 실거래가, 개방대장)
data/processed/  전처리·집계 결과 (동 코드 기준 테이블)
scripts/         00~09 파이프라인 스크립트
outputs/         지도·표·최종 산출물
docs/            설계서(ANALYSIS_DESIGN.md), 데이터 명세
```

## 파이프라인 (scripts/ 순서)

00_download_external.py  외부 데이터 다운로드
01_build_spatial_db.py   250m 격자 공간DB (총인구 0 격자 제거)
02_make_variables.py     건물→격자·동 변수 집계
03_jeonse_ratio.py       실거래가 결합→전세가율 (핵심·까다로움)
04_normalize_kde.py      MinMax 정규화 + KDE
05_vulnerability_index.py S-JVWI 취약도 지수
06_kmeans_cluster.py     위험 유형 군집
07_validate_gu.py        동→구 집계→피해CSV 교차검증
08_detect_next.py        저피해·고위험 동 탐지
09_visualize.py          Folium 히트맵

각 스크립트는 독립 실행 가능하게 짠다. 중간 산출물은 data/processed에 저장.

## 기술 스택

- Python 3.11+
- geopandas, shapely, pyproj (공간분석)
- pandas, numpy
- scikit-learn (MinMaxScaler, KMeans)
- scipy.stats (KDE, 상관분석)
- folium, matplotlib (시각화)

설치: `pip install geopandas pandas scikit-learn scipy folium matplotlib`

## 자주 하는 실수 (피할 것)

- 격자 100m로 만들면 빌라 데이터가 빈 격자 천지가 됨 → 250m 권장
- 실거래가 매매/전세를 건물 단위로 1:1 매칭하려다 데이터 대부분 버림
  → 동 단위 평균 비율로 집계하는 게 안전
- 자치구 25개로 RandomForest 돌리면 무의미 → 검증은 상관분석만
- 도로명주소 → 행정동 변환 시 법정동/행정동 혼동 주의

## 현재 진행 상태

- [ ] 00 외부 데이터 다운로드
- [ ] 01 격자 DB 구축
- [ ] 02 변수 집계
- [ ] 03 전세가율
- [ ] 04 정규화/KDE
- [ ] 05 취약도 지수
- [ ] 06 군집
- [ ] 07 검증
- [ ] 08 다음위험지역 탐지
- [ ] 09 시각화

작업 완료 시 이 체크박스를 갱신할 것.
